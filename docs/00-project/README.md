# fun5

> Project managed by Evo Trident framework.

## Quick Start

```bash
# Setup framework
powershell -ExecutionPolicy Bypass -File .\evo\sync-super-framework.ps1
```

## Project Structure

| Directory | Purpose |
|-----------|---------|
| `evo/` | Framework: workflows, templates, registry |
| `_bmad/` | BMAD modules (AI process engine) |
| `_bmad-output/` | BMAD draft artifacts |
| `design-artifacts/` | WDS UI/UX designs |
| `docs/` | Approved documentation (industry-standard) |
| `_upstream/` | Git submodules (bmad-method, superpowers, antigravity-kit) |

## Workflows

```
/create    Product lifecycle (brief → PRD → UX → architecture → epics)
/dev       Smart development (TDD, auto-verify)
/test      Smart testing (auto-detect framework)
/review    Code review (3-layer adversarial)
/deploy    Deployment (10-point pre-flight)
/incident  Incident response (triage → postmortem)
/plan      Sprint planning + retro
/enhance   Brownfield development + change management
```

## Documentation

See `docs/DOCUMENT-INDEX.md` for all project documents.
Templates: `evo/templates/docs/` (24 industry-standard templates).
